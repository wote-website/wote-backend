{% extends 'base.html.twig' %}

{% block title %}Classification{% endblock %}

{% block body %}
    <h1>Classification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classification.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ classification.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ classification.description }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ classification.creationDate ? classification.creationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>ModificationDate</th>
                <td>{{ classification.modificationDate ? classification.modificationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('classification_index') }}">back to list</a>

    <a href="{{ path('classification_edit', {'id': classification.id}) }}">edit</a>

    {{ include('classification/_delete_form.html.twig') }}
{% endblock %}
