{% extends 'base.html.twig' %}

{% block title %}Edit Profile{% endblock %}

{% block body %}
    <h1>Edit Profile {{ profile.id }}</h1>

    {{ include('profile/_form.html.twig', {'button_label': 'Update'}) }}


    <p>Weightings sum that will be saved at profile update : {{ weightingsSum.sumValue }}</p>

    <a href="{{ path('profile_index') }}">back to list</a>

    {{ include('profile/_delete_form.html.twig') }}

	<a href="{{ path('rated_country_index', {'profileId': profile.id}) }}">voir le classement des pays suivant ce profil</a>

    {{ include('priority/index.html.twig') }}
    {{ include('criterion/sub_index.html.twig') }}


{% endblock %}
